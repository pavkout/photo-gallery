import { IState } from '../types';

export const initialState: IState = {
  isSettingsOpen: false,
  isRealImages: false,
  isHorizontal: false,
  isRandomSize: false,
  gutters: 15,
  columnWidth: 150,
};
